# Ku10 (RenjuClass 在线课程) 项目现状总结与重构建议

本项目是一个基于 jQuery 和 Layui 的旧式 Web 应用，主要功能是播放五子棋（连珠）课程录像并提供交互式棋盘分析工具。

## 1. 核心技术栈
- **前端框架**: jQuery 1.12.4
- **UI 框架**: Layui (用于布局和基础样式)
- **数据格式**: JSON (用于课程记录和配置)
- **资源**: 静态图像 (棋盘背景、棋子、表情等)

## 2. 布局与功能模块 (index.html)
- **棋盘展示区 (`#board_main`)**:
    - 采用 15x15 的 DOM 网格实现。
    - 使用背景图渲染棋盘网格 (`/images/board-0.png`)。
    - 棋子通过在单元格 `div` 上添加 `.black` 或 `.white` 类并配合背景图展示。
- **聊天/日志区 (`#chat_content_list`)**:
    - 实时显示录像中的对话（TALK）和落子信息（MOVE）。
    - 支持教师/大师角色的特殊样式。
- **课程列表 (`#lesson_list`)**:
    - 从 `trans.json` 加载，动态生成课程导航链接。
- **播放控制区 (`#play_control`)**:
    - 提供播放、暂停、倍速切换（1x 到 100x）以及进度条（百分比跳转）。

## 3. 技术实现思路

### 3.1 棋盘逻辑 (`boardObj`)
- **坐标系统**: 采用十六进制表示法，例如 `88` 代表 (8, 8)，对应棋盘中心。
- **状态维护**: 
    - `currgame`: 当前局面的所有落子坐标字符串。
    - `endgame`: 录像预设的最终局面。
    - `curr_step`: 当前手数。
    - `curr_color`: 下一手的颜色。
- **DOM 操作**: 动态创建 `div` 行和列，通过 `addClass` 和 `removeClass` 切换棋子状态。

### 3.2 播放引擎 (`recordPlayer`)
- **动作驱动**: 核心是一个动作队列，包含 `RESET`, `LOAD`, `TALK`, `MOVE`, `EMOTE` 等。
- **时间调度**: 使用 `setTimeout` 结合动作对象中的 `time_int` 实现相对时间的精准播放。
- **快进/跳转**: 跳转进度时，通过清除当前状态并快速重放所有动作至目标位置来实现。

### 3.3 数据结构
- **课程清单 (`trans.json`)**: 
    ```json
    { "show_name": "标题", "data": "文件名.json", "origin": true }
    ```
- **录像数据 (`json/*.json`)**:
    ```json
    { "action": "MOVE", "user": "用户", "content": "坐标", "time_int": 时间戳 }
    ```

### 3.4 分析模式 (`board.html` & `board.js`)
- **交互性**: 允许用户点击棋盘手动落子，支持右键悔棋。
- **持久化**: 局势状态实时反映在 URL Hash 中，方便分享特定局面。

## 4. 覆盖特性
- **动态播放**: 完美还原讲课时的语音/文字同步落子过程。
- **多倍速播放**: 支持极高倍速，方便快速回顾。
- **手动分析**: 随时从录像状态切入手动分析模式。
- **自适应布局**: 利用 Layui 实现了一定程度的响应式布局。

## 5. 后续重构建议 (面向未来)
- **组件化**: 使用 Vue.js 或 React 将棋盘、播放器、列表组件化，解耦逻辑。
- **Canvas 渲染**: 目前使用大量 DOM 节点渲染棋盘，在移动端或老旧浏览器性能受限，建议重构为 Canvas 绘制。
- **状态管理**: 使用现代状态管理（如 Pinia 或 Redux）替代散落在 `boardObj` 中的变量。
- **TypeScript**: 引入类型系统，规范棋盘坐标和动作类型。
- **音效支持**: 现有代码中有声音播放的注释，可正式引入音效库。
- **资源优化**: 棋子图片 (`w_combine.png`) 可以优化为 SVG 以支持更高清的显示。
